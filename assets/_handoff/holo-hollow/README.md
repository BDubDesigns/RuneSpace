# Holo Hollow asset handoff (issue #159)

Temporary staging for the two approved Holo Hollow asset bundles from
Brandon, pending the implementation plan's decision on final placement.

- `runespace-holo-hollow-backgrounds.zip`
- `runespace-holo-hollow-npc-portraits.zip`

These are approved final assets (see issue #159 "Approved asset/content
handoff"). During implementation they should be extracted and placed
following the existing masters/derivatives convention documented in
`docs/portrait-asset-inventory.md` (canonical high-res masters outside
`public/`, optimized committed derivatives under `public/`), and this
`_handoff/` directory should be removed once assets are integrated.

Do not treat anything in this directory as final production content.
